kind = "web"
previewPath = "/"
title = "Zaiah — Evento Presencial"
version = "1.0.0"
id = "artifacts/zaiah-landing"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 3000

[services.development]
run = "PORT=3000 node server.js"

[services.production]
run = "node artifacts/zaiah-landing/server.js"

[services.env]
PORT = "3000"
BASE_PATH = "/"
